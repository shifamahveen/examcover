<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 flex items-center justify-center min-h-screen">

  <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-md">

    <% if (error) { %>
      <div class="bg-red-500 text-white px-4 py-2 mb-4 rounded">
        <%= error %>
      </div>
    <% } %>

    <% if (message) { %>
      <div class="bg-teal-500 text-white px-4 py-2 mb-4 rounded">
        <%= message %>
      </div>
    <% } %>

    <h2 class="text-3xl text-white font-semibold mb-6 text-center">Login</h2>

    <form method="POST" action="/api/v1/auth/login" class="space-y-4">

      <input 
        type="email" 
        name="email" 
        placeholder="Email" 
        class="w-full bg-gray-700 text-white border border-gray-600 px-4 py-2 rounded focus:outline-none focus:ring-2 focus:ring-teal-400" 
        required
      />

      <input 
        type="password" 
        name="password" 
        placeholder="Password" 
        class="w-full bg-gray-700 text-white border border-gray-600 px-4 py-2 rounded focus:outline-none focus:ring-2 focus:ring-teal-400" 
        required
      />

      <button 
        type="submit"
        class="w-full bg-teal-500 hover:bg-teal-600 text-white semibold py-2 rounded transition duration-200"
      >
        Login
      </button>

      <span class="block text-center mt-4 text-gray-400 text-sm mb-0">
        Don't have an account? 
        <a href="/api/v1/auth/register" class="text-teal-400 hover:underline">Register</a>
      </span>
      <span class="block text-center text-gray-400 text-sm p-0" style="margin-top: 5px;">
        Forgot Password? 
        <a href="/api/v1/auth/forgot-password" class="text-teal-400 hover:underline">Click Here</a>
      </span>

    </form>

  </div>

</body>
</html>