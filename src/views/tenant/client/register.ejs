<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Register Client</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="flex items-center justify-center min-h-screen bg-gray-100">

<div class="w-full max-w-md p-8 space-y-6 bg-white rounded-lg shadow-lg">
  <h1 class="text-2xl font-bold text-center">Register New Client</h1>

  <% if (error) { %>
    <div class="p-3 text-red-600 bg-red-100 rounded">
      <%= error %>
    </div>
  <% } %>

  <form action="/api/v1/tenant/clients" method="POST" class="space-y-4">
    <div>
      <label class="block mb-1 font-medium">Client Name</label>
      <input type="text" name="name" required class="w-full p-2 border rounded" />
    </div>
    <div>
      <label class="block mb-1 font-medium">Client Email</label>
      <input type="email" name="email" required class="w-full p-2 border rounded" />
    </div>
    <input type="hidden" name="tenantId" value="<%= tenantId %>">
    <button type="submit" class="w-full p-2 font-semibold text-white bg-blue-600 rounded hover:bg-blue-700">Register</button>
  </form>
</div>

</body>
</html>
