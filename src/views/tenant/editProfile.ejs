<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 flex items-center justify-center min-h-screen">

  <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-md text-center">

    <form action="/api/v1/tenant/profile/edit" method="POST">
        <input type="hidden" name="id" value="<%= tenant.id %>">
        <input type="text" name="name" value="<%= tenant.name %>" required />
        <input type="email" name="email" value="<%= tenant.email %>" required />
        <button type="submit">Save Changes</button>
      </form>
      
  </div>

</body>
</html>