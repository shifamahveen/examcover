<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Forgot Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 flex items-center justify-center min-h-screen">
  <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-md">
    <h2 class="text-3xl text-white font-semibold mb-6 text-center">Forgot Password</h2>
    <% if (flash.success) { %>
      <div class="bg-green-100 text-green-800 p-4 rounded mb-4">
        <%= flash.success %>
      </div>
    <% } %>
    
    <% if (flash.error) { %>
      <div class="bg-red-100 text-red-800 p-4 rounded mb-4">
        <%= flash.error %>
      </div>
    <% } %>    

    <!-- Form to submit email -->
    <form method="POST" action="/api/v1/auth/forgot-password" class="space-y-4">
      <input 
        type="email" 
        name="email" 
        placeholder="Enter your email" 
        class="w-full bg-gray-700 text-white border border-gray-600 px-4 py-2 rounded focus:outline-none focus:ring-2 focus:ring-teal-400"
        required
      />

      <button 
        type="submit"
        class="w-full bg-teal-500 hover:bg-teal-600 text-white font-semibold py-2 rounded transition duration-200"
      >
        Send Reset Link
      </button>
    </form>
  </div>
</body>
</html>