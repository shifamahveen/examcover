<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Tenants</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-800 text-white min-h-screen">

  <div class="container mx-auto p-8">
    <h1 class="text-4xl font-bold text-teal-400 mb-6">Tenants List</h1>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% tenants.forEach(tenant => { %>
        <div class="bg-gray-900 p-6 rounded-lg shadow-lg hover:shadow-2xl">
          <h2 class="text-xl font-semibold text-teal-400"><%= tenant.name %></h2>
          <p class="text-teal-300"><%= tenant.email %></p>
          <a href="/api/v1/admin/tenants/<%= tenant.id %>" class="mt-4 inline-block text-teal-400 hover:text-teal-500">View Details</a>
        </div>
      <% }) %>
    </div>
  </div>

</body>
</html>
